# ── IMPORTANT ────────────────────────────────────────────────────────────────
#
# This file is NOT the active compose file.
#
# The real docker-compose.yml lives in CafeConnWeb/ and is the one that
# actually runs. All docker compose commands must be run from that directory:
#
#   cd CafeConnWeb
#   docker compose up -d
#
# See CafeConnWeb/RUNBOOK.md for the full procedure.
#
# ─────────────────────────────────────────────────────────────────────────────
