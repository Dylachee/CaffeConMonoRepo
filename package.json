{
  "name": "cafeconnect-e2e",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium",
    "e2e:ui": "playwright test --ui",
    "e2e:seed": "cd CafeConnWeb && docker compose exec web python manage.py seed_e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
