/// Design tokens — corner radii.
class AppRadius {
  const AppRadius._();

  static const card = 18.0; // table cards, order cards
  static const cardLg = 20.0; // table grid cards
  static const button = 14.0; // buttons
  static const sheet = 24.0; // bottom sheets (top corners only)
  static const chip = 10.0; // status chips, category chips
  static const avatar = 14.0; // squircle avatars (not circles)
  static const input = 12.0; // text inputs
  static const table = 20.0; // table grid cards
}
